DATABASE_URL="postgresql://exptrack:exptrack_dev_pw@localhost:5432/exptrack_dev"
JWT_ACCESS_SECRET="change_me"
JWT_REFRESH_SECRET="change_me"
PORT=8002
UPLOAD_DIR="./uploads"
# Comma-separated list of allowed browser origins. Leave unset to allow all (dev only).
# CORS_ORIGIN="https://exptrack.example.com"
